{
  "_comment_event": "Event name 'afterFileEdit' is pinned from Cursor docs at spec time (2026-05-22). If Cursor renames this event, update the 'event' field below — the script and Makefile contract are unchanged. See specs/014-lint-on-edit-hook/research.md §4.",
  "_comment_coverage_gap": "PostToolUse Edit|Write does NOT cover files written via the Bash tool. See .claude/settings.json for the same note.",
  "hooks": [
    {
      "event": "afterFileEdit",
      "command": "${workspaceFolder}/hooks/lint-on-edit.sh",
      "timeout": 120,
      "env": {
        "LINT_HOOK_HOST": "cursor"
      }
    }
  ]
}
