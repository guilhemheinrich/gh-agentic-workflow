1. SEVERITY major · LOCATION skills/grievances/scripts/grievances.py:669-686 · WHAT BREAKS `--force` can no longer declare a second entry with the same `--short`, despite contracts/cli-commands.md calling that an unchanged guarantee. Identical shorts always derive the same id (FR-003), so the path that skips the duplicate-short guard then dies in `derive_id` / uniqueness. · TRIGGER `add --short "<existing open short>" --force` with no `--id`. · SUGGESTED FIX Treat `--force` as insufficient: require a distinct `--id` after a short collision, and rewrite the “unless forced” sentence in the contract and T014.

2. SEVERITY major · LOCATION specs/015-grievance-slug-ids/tasks.md:T014 · WHAT BREAKS An explicit `--id` that is already in the ledger is written, because T014 only says “validate `--id` when supplied / otherwise `derive_id`”. The taken-set check lives only inside `derive_id`, which that path never calls. Next `load` then raises “appears twice”. · TRIGGER `add --id GRV-worker-api` when that id already exists. · SUGGESTED FIX In T014, refuse a supplied id that is in the ledger (same remedies as derivation collision) before `append`.

3. SEVERITY major · LOCATION specs/015-grievance-slug-ids/tasks.md:T004 · WHAT BREAKS FR-004/FR-006’s 40-character ceiling is not in `LEGACY_ID_RE`/`SLUG_ID_RE`/`ID_RE` or in T007–T008. `check_id` will accept an over-long supplied id; `Ledger.load` will accept it too once `BLOCK_RE` uses the same pattern. · TRIGGER `--id GRV-abcdefghij-klmnopqrst-uvwxyzabcd-efghijklmn` (4 segments, 47 chars). · SUGGESTED FIX Enforce `len(id) <= 40` in shape validation (not only in `derive_id`) and add that case to T007/T012.

4. SEVERITY major · LOCATION specs/015-grievance-slug-ids/data-model.md (Derivation steps 4–5) · WHAT BREAKS Two surviving tokens that already exceed 40 characters cannot be truncated “at a word boundary” without going below 2 segments, so derivation emits an illegal id instead of refusing (spec edge case “never past the identifier length ceiling”). · TRIGGER `--short "supercalifragilisticexpialidocious antidisestablishmentarianism"`. · SUGGESTED FIX After the “never below 2” trim, if the assembled id is still longer than 40, refuse and tell the caller to pass `--id`.

5. SEVERITY major · LOCATION specs/015-grievance-slug-ids/tasks.md:T006 · WHAT BREAKS The stated order is fold/lowercase, then “keep if uppercase in the original”. Followed literally, the acronym exception is impossible and `DB naming` is refused — the D2 regression. · TRIGGER `--short "DB naming"` (and `API` / `2FA` / `OIDC` in the research probes). · SUGGESTED FIX Classify acronyms against the pre-fold tokens (or a parallel case-preserving split), then lowercase.

6. SEVERITY major · LOCATION specs/015-grievance-slug-ids/tasks.md:T004 · WHAT BREAKS The same unanchored pattern is prescribed for `ID_RE` as for the `BLOCK_RE` group. `check_id` uses `ID_RE.match` (grievances.py:391), which is prefix-only without `$`, so five-segment and suffix-padded ids pass. T007 never tests `GRV-a-b-c-d-e`. · TRIGGER `--id GRV-a-b-c-d-e` or `GRV-worker-api-extra`. · SUGGESTED FIX Keep `^…$` (or `fullmatch`) on `ID_RE`/`check_id`; use the unanchored form only inside `BLOCK_RE`.

7. SEVERITY major · LOCATION specs/015-grievance-slug-ids/data-model.md (Derivation step 2) / tasks.md:T006 · WHAT BREAKS `STOP_WORDS` is never enumerated. A normal English list that omits `will`/`not`/`with` mints the wrong id for the contract’s primary example. T005 does not pin that string. · TRIGGER `--short "worker co-hosted with the API will not scale"` must be `GRV-worker-hosted-api-scale` (contracts/cli-commands.md add table). · SUGGESTED FIX Publish the stop list (at least what the D2 probes require: `the`, `a`, `with`, `will`, `not`, …) and assert those probe outputs in T005.

8. SEVERITY minor · LOCATION specs/015-grievance-slug-ids/plan.md (touchpoint table, `ID_RE` row) · WHAT BREAKS Cited line 82 is `COMMIT_RE`, not `ID_RE` (`ID_RE` is grievances.py:83). T004 repeats “widen `ID_RE` (line 82)”. · TRIGGER An implementer applying the table by line number rewrites commit-hash validation and leaves sequential `ID_RE` in place. · SUGGESTED FIX Point the touchpoint and T004 at line 83 / the `ID_RE` assignment.
