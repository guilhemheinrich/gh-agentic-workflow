1. **blocker · LOCATION `specs/015-grievance-slug-ids/spec.md:31-38`, `specs/015-grievance-slug-ids/data-model.md:38-46`, `specs/015-grievance-slug-ids/tasks.md:115-121` · WHAT BREAKS** The derivation algorithm does not satisfy US1’s claim that distinct sibling-branch declarations receive different identifiers. It keeps only the first four significant tokens, and the collision check sees only each branch’s local ledger, so both declarations can succeed and the merged ledger then fails `Ledger.load` as a duplicate. · **TRIGGER** Branch A declares `"worker queue retry bug alpha"` and branch B declares `"worker queue retry bug omega"`; both derive `GRV-worker-queue-retry-bug`. · **SUGGESTED FIX** Either add deterministic distinguishing material derived from the full description, or weaken the sibling-branch guarantee and define a manual collision-resolution path for distinct descriptions.

2. **major · LOCATION `skills/grievances/scripts/grievances.py:208-244`, `specs/015-grievance-slug-ids/tasks.md:70-74`, `specs/015-grievance-slug-ids/data-model.md:24-30` · WHAT BREAKS** The planned widened `BLOCK_RE` silently skips malformed grievance blocks rather than validating and rejecting them, contradicting the claimed validation “when read back from a JSON header.” `Ledger.load` only iterates matches and has no check for grievance markers that failed the restrictive regex, so commands succeed with the entry absent from the model and generated tables. · **TRIGGER** A block beginning `<!-- grievance:GRV-worker {...} -->` or using an overlong/malformed identifier is present in the ledger. · **SUGGESTED FIX** Parse candidate grievance blocks with a broader marker pattern, then call identifier validation and report malformed IDs; alternatively detect any grievance opener not consumed by `BLOCK_RE`.

3. **major · LOCATION `specs/015-grievance-slug-ids/tasks.md:70-74`, `skills/grievances/scripts/grievances.py:77-83`, `skills/grievances/scripts/grievances.py:208-244` · WHAT BREAKS** T004’s exact regex, `GRV-(?:\d{4}|[a-z0-9]+(?:-[a-z0-9]+){1,3})`, does not enforce the documented 40-character ceiling. Because `Ledger.load` never calls `check_id`, an overlong slug in a JSON block is accepted on read even if command-line validation later adds a separate length check; the listed tests also contain no overlong supplied-ID case. · **TRIGGER** An identifier such as `GRV-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa-bb` in a ledger, or supplied through `--id` if T008 implements only the specified `ID_RE`. · **SUGGESTED FIX** Enforce `len(id) <= 40` in a shared validator called from both `cmd_add` and `Ledger.load`, and add explicit overlength tests for both boundaries.

4. **major · LOCATION `specs/015-grievance-slug-ids/spec.md:132-134`, `specs/015-grievance-slug-ids/spec.md:199-202`, `specs/015-grievance-slug-ids/contracts/cli-commands.md:51-72`, `specs/015-grievance-slug-ids/tasks.md:109-112` · WHAT BREAKS** The explicit-ID contract contradicts FR-004 and SC-004. The requirements say supplied identifiers have segments drawn from the grievance description and that every newly minted identifier contains words from that description, while T014 only shape-validates `--id` and the contract allows any free descriptive identifier. · **TRIGGER** `add --short "database naming mismatch" --id GRV-worker-api ...` is accepted by the planned implementation although neither segment comes from the short description. · **SUGGESTED FIX** Decide whether explicit IDs are exempt from derivation. Either state that exemption in FR-004/SC-004, or require and test that supplied segments correspond to the normalized description.

5. **minor · LOCATION `specs/015-grievance-slug-ids/tasks.md:154-166` · WHAT BREAKS** T024 cannot follow the task list’s own tests-first requirement. Before conflict detection exists, the bare-`=======` test already passes; no task introduces the “naive detection” against which it is supposed to fail before T027 implements the correct detector. · **TRIGGER** Execute T023–T025 in order against the current source: T024 is green immediately rather than “Must FAIL.” · **SUGGESTED FIX** Remove the false fail-first claim, or first introduce/test a detector API whose absent implementation makes both positive and negative marker tests fail for the intended reason.

6. **minor · LOCATION `specs/015-grievance-slug-ids/tasks.md:70-82`, `specs/015-grievance-slug-ids/data-model.md:38-46` · WHAT BREAKS** `STOP_WORDS` is required but never defined by any artefact, so the derivation contract is incomplete and different conforming implementations can mint different identifiers while passing all listed examples. · **TRIGGER** Descriptions containing unclassified candidates such as `"from"`, `"after"`, or `"that"` produce different slugs depending on the implementer’s unstated stop-word list. · **SUGGESTED FIX** Publish the exact immutable stop-word set in the data model or research decision and pin representative inclusion and exclusion cases in tests.
