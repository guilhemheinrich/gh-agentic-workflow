{
  "_comment_template": "Template — copy to .cursor/hooks.json in the consumer repo. Pairs with hooks/lint-on-edit.sh and the `lint:` recipe in the Makefile (see ../SKILL.md). Adjust the command path if you store the hook elsewhere.",
  "_comment_event": "Event name 'afterFileEdit' is pinned from Cursor docs at spec time (2026-05). If Cursor renames this event, update the 'event' field below — the script and Makefile contract are unchanged.",
  "_comment_coverage_gap": "afterFileEdit does NOT cover files written via the Bash tool. Complement with a pre-commit git hook for full coverage.",
  "hooks": [
    {
      "event": "afterFileEdit",
      "command": "${workspaceFolder}/hooks/lint-on-edit.sh",
      "timeout": 120,
      "env": {
        "LINT_HOOK_HOST": "cursor"
      }
    }
  ]
}
